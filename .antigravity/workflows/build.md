---
description: Build llama.cpp using CMake
---

1. Create a build directory
```bash
mkdir -p build
```

2. Configure with CMake
// turbo
```bash
cmake -B build
```

3. Build the project
// turbo
```bash
cmake --build build --config Release
```
